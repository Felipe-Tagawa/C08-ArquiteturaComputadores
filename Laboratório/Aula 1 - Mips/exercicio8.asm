# (1 + 5 - 2)

li $t0, 1
addi $t0, $t0, 5 # (1 + 5)
subi $t0, $t0, 2 # (6 - 2)

# (32 / $t0)

li $t1, 32

div $t1, $t0 # (32 / 4)
mflo $t0 # (8)

li $t1, 1
add $t0, $t1, $t0 # (1 + 8)

li $t1, 22

sllv $t1, $t1, $t0 # ($t1 << $t0 --> 22 << 9)

